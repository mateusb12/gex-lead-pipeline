APP_ENV=dev

# Sem .env, o docker compose usa APP_ENV=production e exige SMS_WEBHOOK_URL.
# Este arquivo ativa modo dev para facilitar testes locais.

# Se estiver vazio em APP_ENV=local/dev/debug,
# o sms-distributor cria automaticamente uma URL real do webhook.site.
SMS_WEBHOOK_URL=
